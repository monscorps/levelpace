https://levelpace.andustemme.workers.dev
#
# Non-comment lines, in order:
#   1  where to SEND your stats. This is also where the rankings and the
#      global baseline are read back from -- one address, always current.
#   2  submission token, if the board uses one   (optional; the build adds it)
#   3  the web board, named by its baseline file. The companion reads
#      version.json from beside it and "Open the web board" goes here.
#
# Only whoever hands this folder out edits it. You do not need to touch it.

https://monscorps.github.io/levelpace/api/baseline.json
